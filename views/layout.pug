doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{pageTitle}
        link(rel="stylesheet", href="/css/layout.css")
        block styles
    body 
        header 
            h1.app-logo Hotel Booking App
            nav.app-header-links 
                ul 
                    li 
                        a(href="/" class=(path==='/' && 'active-link')) Home
                    li 
                        a(href="/all-listings" class=(path==='/all-listings' && 'active-link')) Listings
                    li 
                        a(href="/book-now" class=(path==='/book-now' && 'active-link')) Book now
                    li
                        a(href="/admin" class=(path==='/admin' && 'active-link')) Admin bookings
        block contents